version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # The build command creates a 'build' directory.
    baseDirectory: build 
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*